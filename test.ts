// Two types -> Explicit (Manual) and implicit also called type inference (Automatic)
let x:number = 10;
let y:number = 20;

console.log(x+y);
// if we doesnt declare variable type it will automatic take "any" type implicitly
let a = 1;
let b = 2;
console.log(a+b);